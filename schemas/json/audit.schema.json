{
  "description": "One attributable mutation, per P8: one rule id, one transform name.\n\nHolds the pre-image hash so a value can be verified against the Bronze\npartition that still holds it (\u00a74.2). The hash proves which value it was;\nBronze produces it.",
  "properties": {
    "row_ordinal": {
      "description": "Position of the source row in its Bronze partition.",
      "minimum": 0,
      "title": "Row Ordinal",
      "type": "integer"
    },
    "record_key": {
      "title": "Record Key",
      "type": "string"
    },
    "column_name": {
      "title": "Column Name",
      "type": "string"
    },
    "rule_id": {
      "title": "Rule Id",
      "type": "string"
    },
    "transform_name": {
      "title": "Transform Name",
      "type": "string"
    },
    "pre_image_hash": {
      "title": "Pre Image Hash",
      "type": "string"
    },
    "post_image_hash": {
      "title": "Post Image Hash",
      "type": "string"
    }
  },
  "required": [
    "row_ordinal",
    "record_key",
    "column_name",
    "rule_id",
    "transform_name",
    "pre_image_hash",
    "post_image_hash"
  ],
  "title": "AuditRecord",
  "type": "object"
}
